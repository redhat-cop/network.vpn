---
profile: production
exclude_paths:
  - changelogs/changelog.yaml
skip_list:
  - sanity[cannot-ignore]  # some sanity ignores needed due to problems in azure.azcollection
  - var-naming[no-role-prefix]  # this rule breaks how network validated content works
